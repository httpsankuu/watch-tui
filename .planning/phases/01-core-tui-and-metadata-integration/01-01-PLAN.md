---
phase: 01-core-tui-and-metadata-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - main.go
  - config/config.go
  - tmdb/client.go
  - tmdb/models.go
  - tui/app.go
  - tui/styles.go
  - tui/components/search.go
  - tui/components/list.go
  - tui/components/details.go
  - tui/image.go
autonomous: true
requirements: ["CORE-01", "CORE-02", "CORE-03", "META-01", "META-02", "META-03"]
user_setup:
  - service: TMDB API
    why: "Requires an API key to fetch movie and TV show metadata."
    env_vars:
      - name: TMDB_API_KEY
        source: "TMDB Settings -> API -> API Key (v3 auth)"

must_haves:
  truths:
    - "User can instantly search for titles"
    - "User can navigate categories (movies, shows, anime, trending) using slash commands"
    - "User can navigate the UI exclusively using the keyboard (arrow keys, etc)"
    - "System integrates with TMDB API to fetch high-quality metadata"
    - "User can view visual posters and detailed title views within the terminal"
    - "User can select visual themes (Modern, ASCII)"
    - "FLAGGED ASSUMPTION (META-01 probe): If TMDB fetch is interrupted or run in parallel, it is guaranteed to cancel safely using context.Context without crashing or leaking goroutines."
  artifacts:
    - path: "main.go"
      provides: "Entrypoint and Bubble Tea initialization"
    - path: "tmdb/client.go"
      provides: "TMDB API integration handling HTTP requests and JSON parsing"
    - path: "tui/app.go"
      provides: "Core Bubble Tea Model and routing state"
    - path: "tui/styles.go"
      provides: "Lipgloss definitions for Modern vs ASCII themes"
  key_links:
    - from: "tui/app.go"
      to: "tmdb/client.go"
      via: "Calls TMDB client methods within Bubble Tea Cmds"
---

<objective>
Establish the foundational Bubble Tea terminal UI, keyboard-first navigation patterns, TMDB API integration, and rendering of complex UI elements (posters, detailed views).

Purpose: Creates the core TUI framework that all future playback and download features will plug into.
Output: A functional terminal application where users can browse, search, and view metadata for movies and TV shows.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-tui-and-metadata-integration/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Setup and TMDB API Client</name>
  <files>go.mod, main.go, tmdb/client.go, tmdb/models.go, config/config.go</files>
  <action>Initialize the Go module `github.com/watch-it`. Implement the TMDB client (`tmdb/client.go`) that fetches trending media and searches for titles using `net/http`. Ensure it parses JSON into structs defined in `tmdb/models.go`. Implement `config/config.go` to load the TMDB API key from the `TMDB_API_KEY` environment variable. Ensure the TMDB client methods accept `context.Context` to handle interruptions safely (addressing META-01 concurrency probe). Set up `main.go` to validate config and start a basic Bubble Tea program.</action>
  <verify>go mod tidy && go build</verify>
  <acceptance_criteria>
    - go.mod exists with module `github.com/watch-it`
    - `tmdb/client.go` methods accept `context.Context`
    - `config/config.go` reads `TMDB_API_KEY`
  </acceptance_criteria>
  <done>TMDB client is functional, context-aware, and the app builds.</done>
</task>

<task type="auto">
  <name>Task 2: Core UI, Theming, and Routing</name>
  <files>tui/app.go, tui/styles.go, tui/components/search.go, tui/components/list.go</files>
  <action>Implement the main Bubble Tea model in `tui/app.go` with a routing mechanism (states: browse, search). Implement slash command interception (e.g., `/movies`, `/shows`) in the global `Update` loop to switch categories. Create `tui/styles.go` using Lipgloss to define two themes ("Modern" with rich colors/unicode borders, and "ASCII" with basic characters). Implement the search component with debouncing using `charmbracelet/bubbles/textinput`, and a list component using `charmbracelet/bubbles/list` for browsing.</action>
  <verify>go build -o watch-it.exe main.go</verify>
  <acceptance_criteria>
    - `tui/app.go` contains slash command parsing (`/` key interception)
    - `tui/styles.go` defines Modern and ASCII themes
    - Keyboard navigation (arrow keys) works for lists
  </acceptance_criteria>
  <done>UI routing and styling foundation is laid.</done>
</task>

<task type="auto">
  <name>Task 3: Details View and Terminal Poster Rendering</name>
  <files>tui/components/details.go, tui/image.go</files>
  <action>Implement the details view (`tui/components/details.go`) to show metadata for a selected title. Implement terminal image rendering in `tui/image.go` to fetch poster images from TMDB and convert them to terminal block characters (Modern theme) or ASCII art (ASCII theme). Integrate this into the details view so it displays alongside text metadata.</action>
  <verify>go build -o watch-it.exe main.go</verify>
  <acceptance_criteria>
    - `tui/image.go` has logic to convert image pixels to terminal characters
    - `tui/components/details.go` renders metadata and the processed poster
  </acceptance_criteria>
  <done>Detailed views with posters are visible in the terminal.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go build` succeeds without errors.
- [ ] The app initializes correctly with or without TMDB_API_KEY.
- [ ] Ensure all required Bubble Tea dependencies are explicitly listed in go.mod.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- App navigation is fully keyboard accessible
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-tui-and-metadata-integration/01-01-SUMMARY.md`
</output>
